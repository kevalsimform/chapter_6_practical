import 'package:flutter/material.dart';

class AppConstants {
  static const double tabBarHeight = 40;
  static const double horizontalImageHeight = 85;
  static const Color cursorColor = Colors.grey;
}

enum From { horizontalNewsCard, mainNewsCard }
